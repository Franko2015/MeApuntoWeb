@model IEnumerable<Lugar>

@{
    ViewData["Title"] = "Lugares";
    ViewData["Nav"] = "Listado de lugares";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br>
  <br>
  <br>

  <div class="Text text-center">
    <h2><strong>Lugares</strong></h2>
  </div>
  <br>
  <br>


  <div class="container">

    <a asp-action="Create" class="btn btn-outline-primary col-2"><i class="bi bi-person-plus-fill"> Crear Lugar</i></a>
    <br>
    <br>
    <table class="table table-hover table-bordered" style="color: black;">

      <thead>
        <tr>
          <th scope="col">Direccion</th>
          <th scope="col">Accion</th>
        </tr>
      </thead>
      <tbody>
              @foreach (var item in Model)
                {
        <tr>
          <td>@item.Direccion</td>
          <td>
            <div class="my-event-boton">
              <a asp-action="Delete" asp-route-LugarId="@item.Id" class="btn my-btn-action"><i class="material-icons">delete</i></a>

            </div>
          </td>
        </tr>
        }
      </tbody>
    </table>
  </div>

  <br>
  <br>
  <br>
